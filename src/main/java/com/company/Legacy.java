package com.company;

public class Legacy {


    // Jackson code for converting JSON to POJO.
         /*JSONObject pokemon = new JSONObject(response);
        System.out.println(pokemon.get("name"));
        ObjectMapper objectMapper = new ObjectMapper();
        objectMapper.disable(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES);
        JsonNode jsonNode = objectMapper.readTree(response);
        String name = jsonNode.get("name").asText();
        System.out.println(name + " is the name from the JSON string.");
        PokeModel pokeModel = objectMapper.readValue(response, PokeModel.class);
        */
}
